<Window x:Class="IDEngine.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IDEngine"
        mc:Ignorable="d"
        Title="MainWindow"
        Width="1200" Height="700"
        MinWidth="1200" MinHeight="700"
        MaxWidth="1600" MaxHeight="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="600"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="800"/>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
        </Grid.ColumnDefinitions>
        <Canvas x:Name="panel" Background="Black" Grid.Row="0" Grid.Column="0"></Canvas>
        <ListView x:Name="list_maps" Background="LightBlue" SelectionChanged="list_maps_SelectionChanged"
                  Grid.Row="0" Grid.Column="1"></ListView>
        <ListView x:Name="list_points" Background="LightGray" Grid.Row="0" Grid.Column="2"></ListView>

        <Label x:Name="lbl_screen" Grid.Row="1" Grid.Column="0" Background="Green" Foreground="White" Content="Log:"></Label>
        <ListView x:Name="list_screen" Grid.Row="1" Grid.Column="1"></ListView>
    </Grid>
</Window>
